# ---------------------------------------------------------------------------
# CANDIDATE pins for Phase 1 (free Colab T4). These are NOT final.
# The exact resolved versions are captured into requirements.lock by running
# `make lock` on a clean Colab T4 runtime (see README "Reproducibility").
#
# On Colab, torch + CUDA are preinstalled; do NOT reinstall torch there.
# Unsloth is installed via its Colab-specific extra (see notebook).
# ---------------------------------------------------------------------------

# Core (CPU-safe; needed for config + data prep + unit tests)
pydantic>=2.6
pyyaml>=6.0
python-dotenv>=1.0

# Training stack (GPU)
transformers>=4.51.3,<4.58   # >=4.51 required for Qwen3 (else KeyError: 'qwen3')
trl>=0.18.2,<0.25
peft>=0.18.0
bitsandbytes>=0.43
datasets>=2.19
accelerate>=0.34.1

# Unsloth: install separately on Colab (version 2026.6.x), e.g.
#   pip install "unsloth[colab-new]"
# Pinned exactly in requirements.lock after resolution on the target runtime.

# Eval
rouge-score>=0.1.2
bert-score>=0.3.13
evaluate>=0.4.1

# Tracking (optional)
tensorboard>=2.16
